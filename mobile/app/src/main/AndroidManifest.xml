<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".home.DormShareApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DormShare"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Login Screen (Launcher) -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Registration Screen -->
        <activity android:name=".auth.RegisterActivity" />

        <!-- OAuth2 Deep Link Callback (Google Sign-In from Chrome Custom Tab) -->
        <activity
            android:name=".auth.OAuth2CallbackActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="dormshare"
                    android:host="oauth2"
                    android:pathPrefix="/callback" />
            </intent-filter>
        </activity>
        <!-- Main Feature Screens -->
        <activity android:name=".home.HomeActivity" />
        <activity android:name=".expense.ExpensesActivity" />
        <activity android:name=".group.GroupsActivity" />
        <activity android:name=".settlement.SettlementsActivity" />
        <activity android:name=".pantry.PantryActivity" />

        <!-- NEW: Profile & Notifications -->
        <activity android:name=".profile.ProfileActivity" />
        <activity android:name=".notification.NotificationsActivity" />

    </application>
</manifest>
